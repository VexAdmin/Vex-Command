{
  "name": "vex-command",
  "version": "0.1.0",
  "private": true,
  "description": "Vex Command — Founder Console (ops.vexraptor.com)",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "type-check": "vue-tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "pinia": "^2.1.7",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "typescript": "^5.4.0",
    "vite": "^8.2.2",
    "vitest": "^4.1.11",
    "vue-tsc": "^2.0.6"
  }
}
